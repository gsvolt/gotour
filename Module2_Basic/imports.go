package main

import (
	"fmt"
	"math"
)

// can also do imports on individual lines like import "modulename", but the method above is preferred.

func main() {
	fmt.Printf("Now you have %g problems.\n", math.Sqrt(7))
}
